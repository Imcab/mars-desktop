# What builds the documentation, pinned on purpose.
#
# Material for MkDocs warns on every build that MkDocs 2.0 removes the plugin
# system and the theming system with no migration path. An unpinned
# `pip install mkdocs-material` in CI is therefore a build that works until the
# day it does not, for a reason nobody here changed. The upper bounds are the
# whole point of this file; bump them deliberately.
mkdocs-material>=9.7,<10
mkdocs>=1.6,<2
